import { Button } from "./button";

export const components = {
  Button,
  Popover: {
    baseStyle: {
      popper: {
        width: "fit-content",
        maxWidth: "fit-content",
      },
    },
  },
};
